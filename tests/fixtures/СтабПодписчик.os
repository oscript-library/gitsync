// Минимальный стаб подписчика для тестирования МенеджерПодписок.
// Реализует интерфейс ИндексПодписчиков: Приоритет() и ВыполнитьПодписку().
// ЖурналВызовов — массив, переданный по ссылке из теста; стаб пишет в него свою метку при каждом вызове.

Перем _Приоритет;
Перем _Метка;
Перем _ЖурналВызовов;

Процедура Инициализировать(Знач Приоритет, Знач Метка, Знач ЖурналВызовов) Экспорт
	_Приоритет      = Приоритет;
	_Метка          = Метка;
	_ЖурналВызовов  = ЖурналВызовов;
КонецПроцедуры

Функция Приоритет() Экспорт
	Возврат _Приоритет;
КонецФункции

Процедура ВыполнитьПодписку(ИмяПроцедуры, Параметры) Экспорт
	_ЖурналВызовов.Добавить(_Метка);
КонецПроцедуры
